# Build output of `npm run bake`. Regenerable, and 520 KB of noise in diffs.
dist/

# no trailing slash: a symlink is not a directory, and a trailing slash
# would let a symlinked node_modules commit as a broken link.
node_modules
.netlify/
.DS_Store
*.log
